{
  "name": "freedeck-v6-rewrite",
  "version": "1.0.0",
  "description": "mysteriouus..",
  "main": "src/entryChecker.js",
  "scripts": {
    "start": "electron .",
    "companion": "electron . companion",
    "server": "node src/index.js server",
    "debug-server": "node src/index.js server debug",
    "lint": "eslint ** --fix  --ignore-pattern *.css  --ignore-pattern *.mp*  --ignore-pattern *.wav --ignore-pattern *.html --ignore-pattern *.min* --ignore-pattern *.Freedeck --ignore-pattern *.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "asar-bundle-runner": "^1.6.0",
    "electron": "^27.0.0",
    "express": "^4.18.2",
    "formidable": "^3.5.1",
    "readline": "^1.3.0",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "eslint": "^8.55.0",
    "eslint-config-google": "^0.14.0"
  }
}
